import { PharmacyReportModel } from './pharmacy-report-model.model';

describe('PharmacyReportModel', () => {
  it('should create an instance', () => {
    expect(new PharmacyReportModel()).toBeTruthy();
  });
});
